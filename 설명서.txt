node_modules : react가 쓰는 라이브러리를 저장하는 곳
package.json : 패키지가 저장되는 곳, node_modules가 없어도 이 파일이 있으면 라이브러리를 설치함
src : 직접적으로 다루게 될 소스 코드 파일이 담긴 곳
public : 이미지 같은 static 자료를 놓는 곳

public에 있는 index.html에 src의 jsx나 js 파일들이
필요한 부분에 다른 요소를 채워주는 등으로 작동
